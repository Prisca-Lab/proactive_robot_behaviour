## Info
The folder will contain the data obtained from the experiment with the **random decision system**. 
- For each participant, _demographic_ data (i.e. age, gender, education level), responses to the _DGD_ questionnaire, _match history_ and _post-interaction questionnaire_ responses will be saved in a JSON file.
